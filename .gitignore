# ───────── Dependencies ─────────
node_modules/
.pnp
.pnp.js

# ───────── Secrets / env ─────────
# ห้าม commit ค่าจริง — เก็บเฉพาะ *.example เป็นแม่แบบ
.env
.env.*
!.env.example
# มี Figma personal access token อยู่ข้างใน — ห้าม commit
.mcp.json
# Codex CLI config (mcp_servers.figma มี FIGMA_API_KEY จริง) — ห้าม commit
.codex/
# รูป reference ที่ดึงจาก Figma (ไว้ดูประกอบ ไม่ต้องขึ้น repo)
figma-ref/

# ───────── Build output ─────────
dist/
build/
*.tsbuildinfo

# ───────── Vite / cache ─────────
.vite/
*.local

# ───────── Logs ─────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ───────── Editor / IDE ─────────
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# ───────── OS ─────────
.DS_Store
Thumbs.db
desktop.ini
